/// Lets app code declare motion that carries no information — a spinner, a
/// shimmer, a pulsing dot — so a scenario photographs it standing still
/// instead of waiting for it to stop. See [Ambient].
library;

export 'src/ambient/ambient.dart' show Ambient;
